#include <stdio.h>

int main(void){

    char name = 's'; // single quote for single char
    // char *name = "s" // if in future you need to add more characters.
    char *fullname = "soumya ranjan das";

    printf("%c\n", name);
    printf("%s\n" ,fullname);
}